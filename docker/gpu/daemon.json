{
  "_comment": "Phase 16 step 3 — nvidia-container-toolkit runtime registration. Real, complete, UNRUN: this Mac has no NVIDIA GPU and no Linux Docker daemon to install it into (see containers/README.md). Merge this into /etc/docker/daemon.json on the target Linux host after installing nvidia-container-toolkit (https://github.com/NVIDIA/nvidia-container-toolkit) via `nvidia-ctk runtime configure --runtime=docker`, which writes exactly this stanza (shown explicitly here rather than hand-waved) then `systemctl restart docker`.",
  "runtimes": {
    "nvidia": {
      "path": "nvidia-container-runtime",
      "runtimeArgs": []
    }
  }
}
