# symbiyosys — sby-driven formal verification, not CMake-compiled.
# Run: `sby -f axi_nodead.sby` / `sby -f dma_nooverlap.sby`.
# Needs: yosys + SymbiYosys (`sby`) + an SMT solver (z3) on the PATH. See
# README.md for toolchain status.
