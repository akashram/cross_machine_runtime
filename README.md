# cross_machine_runtime

A learning project spanning CPU, GPU, FPGA, TPU, NPU, and analog compute — lock-free data structures, an MLIR compiler, distributed training, SciML, and more.

See `PLAN.md` and `CLAUDE.md` for full scope and status.
