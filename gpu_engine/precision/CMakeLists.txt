# Steps 15-17: Mixed precision + Tensor Core alignment — stub, requires A100/H100
# TODO: uncomment when on GPU hardware
# add_cuda_benchmark(precision_bench precision_bench.cu)
# target_compile_options(precision_bench PRIVATE -arch=sm_80)  # Ampere for BF16
