# Step 23: NVML power monitoring — stub, requires CUDA GPU + NVML on Linux
# TODO: uncomment when on GPU hardware
# find_library(NVML_LIB nvidia-ml)
# add_cuda_benchmark(power_bench power_bench.cu)
# target_link_libraries(power_bench PRIVATE ${NVML_LIB})
