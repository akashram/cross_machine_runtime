# Steps 11-12: Flash Attention forward + backward — stub, requires CUDA GPU
# TODO: uncomment when on GPU hardware
# add_cuda_benchmark(flash_attn_bench flash_attn_bench.cu)
# target_link_libraries(flash_attn_bench PRIVATE CUDA::cudnn)
