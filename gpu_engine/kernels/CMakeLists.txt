# Steps 8-9: Element-wise kernels + GEMM — stub, requires CUDA GPU
# TODO: uncomment when on GPU hardware
# add_cuda_benchmark(elementwise_bench elementwise_bench.cu)
# add_cuda_benchmark(gemm_bench gemm_bench.cu)
# target_link_libraries(gemm_bench PRIVATE CUDA::cublas)
