# Step 6: Memory coalescing validator — stub, requires CUDA GPU + Nsight Compute
# TODO: when on GPU hardware, build a test kernel to exercise the validator script
# add_cuda_benchmark(coalescing_test coalescing_test.cu)
