cmake_minimum_required(VERSION 3.25)

# Phase 8: TPU Backend
# Python/JAX-based — no C++ to compile here.
# CMake only documents the phase structure.
message(STATUS "tpu_engine/ is Python/JAX-based — no CMake targets")
